# Data ingestion (yfinance, Finnhub)
pandas>=1.5.0
numpy>=1.22
yfinance>=0.2.0
requests>=2.28.0

# FastAPI + Swagger
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Scheduling
apscheduler>=3.10.0

# Database (PostgreSQL + TimescaleDB)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
# Or asyncpg>=0.28.0 for async

# Optional: TimescaleDB helpers
# timescaledb (if using TimescaleDB Python client)
