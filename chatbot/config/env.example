# QuantTradingOS Chatbot — copy to .env and set values. Never commit .env.
# OpenAI (required for the LLM)
OPENAI_API_KEY=

# Optional: MCP server connection (defaults assume mcp-server is sibling of chatbot/)
# MCP_SERVER_COMMAND=python
# MCP_SERVER_ARGS=-m,qtos_mcp.server
# MCP_SERVER_CWD=/path/to/QuantTradingOS/mcp-server
